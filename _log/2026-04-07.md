# 2026-04-07 Session Log

## What happened

- **wp-env troubleshooting**: Plugin wasn't showing in admin. Root cause was stale `docker-compose.yml` in `~/.wp-env/` with incorrect mount paths (missing `wordpress-plugins/` segment). Resolved by destroying the stale instance and letting wp-env regenerate the correct `a259a2a` config.
- **Design exploration (grill-me)**: Explored UX improvements for the team member editing experience.
  - Discussed auto-syncing post title with first/last name meta fields — **decided against it** (too complex, not worth the edge cases).
  - Discovered custom `pikari-team/meta` block binding source is just a wrapper around `get_post_meta()` — can be replaced with `core/post-meta` for free bidirectional editing when Gutenberg work begins.
  - **Key decision: V1 targets Classic Editor (WP Bakery site).** Block editor enhancements deferred to v2.
  - Classic Editor meta box is functional for data capture. Next focus is card display output.

## Key decisions

1. V1 release = Classic Editor compatibility. First install site uses WP Bakery.
2. Meta box UX is "good enough" for v1 — captures data correctly.
3. Display surfaces for v1: standalone card page, shortcode embed, vCard, QR code.
4. Block bindings, block templates, Gutenberg UX = v2 milestone.
5. Custom block binding source should be replaced with `core/post-meta` in v2.

## What's next

- Work through display issues on the card output (standalone page, shortcode embed)
- Add `pikari_team_card_template` to Classic Editor meta box
- Ensure all card features work without Gutenberg dependency

## Files changed

- `.wp-env.json` — reverted to original (removed failed `lifecycleScripts` experiment)
