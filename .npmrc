# Release tags are created only by publishing a GitHub Release.
# Without this, `npm version` would create a competing local vX.Y.Z tag in a repo
# where the release tag is already force-moved onto the dist branch.
git-tag-version=false
